COMPOSE_PROJECT_NAME=txrouter
MONGO_HOST=mongodb
MONGO_PORT=27017
MONGO_DATABASE=plugindb
MONGO_COLLECTION=plugincollection
MONGO_INITDB_ROOT_USERNAME=root
MONGO_INITDB_ROOT_PASSWORD=example
MONGO_NON_ROOT_USERNAME=db
## MONGO_NON_ROOT_PASSWORD=collection # not needed? xxx
## MONGODB_DATA_DIR=./build/mongodb/data # not needed? xxx
INIT_PLUGIN_PATH=./build/plugin
MONGO_CONTAINER=txrouter-mongodb
DOCKER_SOCK=/var/run/docker.sock
# this should match the port provided by nginx
TXROUTER_API_PORT=8080
# this is the port for nginx
API_PORT=8080
LOGGING=logging
# SSL_CERT_DIR=
IPAM_CONFIG_SUBNET=10.16.0.0/16
