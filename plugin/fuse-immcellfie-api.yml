version: "3.4"
services:
  fuse-immcellfie-api:
    port: 8080
    image: fuse-immcellfie-api:${fuse_immcellfie_api_TAG}
    environment:
      IMMCELLFIE_HOST: ${IMMCELLFIE_HOST}
      IMMCELLFIE_PORT: ${IMMCELLFIE_PORT}
      IMMCELLFIE_VERSION: ${IMMCELLFIE_VERSION}
      # FUSE_IMMCELLFIE_API_CONFIG: ${FUSE_IMMCELLFIE_API_CONFIG}
      # FUSE_IMMCELLFIE_API_LOGGING: ${FUSE_IMMCELLFIE_API_LOGGING}
